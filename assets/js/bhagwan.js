const esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
const grid=document.querySelector('#grid'),q=document.querySelector('#q'),count=document.querySelector('#count');let active='all',items=[];
function commonsSearchUrl(d){return 'https://commons.wikimedia.org/wiki/Special:MediaSearch?type=image&search='+encodeURIComponent(d.imageQuery||d.name)}
function imageMarkup(d){if(d.image){return `<a class="deity-image has-image" href="${esc(d.imagePage||commonsSearchUrl(d))}" target="_blank" rel="noopener"><img src="${esc(d.image)}" alt="${esc(d.name)}" loading="lazy" onerror="this.parentElement.classList.remove('has-image');this.remove()"><span>Wikimedia Commons</span></a>`}return `<a class="deity-image" href="${esc(commonsSearchUrl(d))}" target="_blank" rel="noopener"><div class="image-placeholder">ॐ</div><span>🖼️ चित्र खोज्नुहोस्</span></a>`}
function render(){const term=q.value.trim().toLowerCase();const filtered=items.filter(d=>(active==='all'||d.category===active)&&(!term||[d.name,d.aliases,d.category,d.power,d.veda,d.story,d.symbols,d.vahana,d.consort,d.mantra,d.temples,d.avatars,d.relationships,d.scriptures].join(' ').toLowerCase().includes(term)));count.textContent=`${filtered.length} देव–देवी देखाइँदैछन्`;grid.innerHTML=filtered.map(d=>`<article class="deity-card">${imageMarkup(d)}<div class="deity-body"><small>${esc(d.category)}</small><h2>${esc(d.name)}</h2><p class="aliases">${esc(d.aliases)}</p><div class="detail"><b>⚡ शक्ति / भूमिका</b><p>${esc(d.power)}</p><b>📖 वेद / ग्रन्थ</b><p>${esc(d.veda)}</p><b>🪔 विस्तृत कथा</b><p>${esc(d.story)}</p>${d.avatars?`<b>🔱 रूप / अवतार</b><p>${esc(d.avatars)}</p>`:''}${d.relationships?`<b>👨‍👩‍👧 सम्बन्ध</b><p>${esc(d.relationships)}</p>`:''}${d.mantra?`<b>📿 मन्त्र</b><p class="mantra-text">${esc(d.mantra)}</p>`:''}${d.temples?`<b>🛕 प्रमुख तीर्थ / मन्दिर</b><p>${esc(d.temples)}</p>`:''}<div class="facts"><span>🔱 ${esc(d.symbols)}</span><span>🦚 ${esc(d.vahana)}</span><span>💮 ${esc(d.consort)}</span></div></div><a class="source" href="${esc(d.source)}" target="_blank" rel="noopener">मूल स्रोत / अध्ययन →</a></div></article>`).join('')||'<div class="empty">खोजीसँग मिल्ने देवता भेटिएन।</div>'}
fetch('../data/deities.json').then(r=>r.json()).then(d=>{items=Array.isArray(d)?d:[];render()}).catch(()=>{grid.innerHTML='<div class="empty">देवता डेटा लोड हुन सकेन।</div>'});q.addEventListener('input',render);document.querySelectorAll('.filter').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');active=b.dataset.cat;render()}));